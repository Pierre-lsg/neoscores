{% extends 'base.html.twig' %}

{% block title %}Target{% endblock %}

{% block body %}
    <h1>Target</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ target.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ target.name }}</td>
            </tr>
            <tr>
                <th>Par</th>
                <td>{{ target.par }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary m-1" href="{{ path('app_target_index') }}">back to list</a>

    <a class="btn btn-primary m-1" href="{{ path('app_target_edit', {'id': target.id}) }}">edit</a>

    {{ include('target/_delete_form.html.twig') }}
{% endblock %}
